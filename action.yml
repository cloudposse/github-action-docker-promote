name: 'Docker promote'
description: 'Promote docker image'
author: hello@cloudposse.com
branding:
  icon: 'tag'
  color: 'white'
inputs:
  organization:
    description: 'Organization'
    required: true
  repository:
    description: 'Repository'
    required: true
  registry:
    description: 'Docker registry'
    required: true
  from:
    required: false
    description: "Source tag"
  to:
    required: false
    description: "Target tags"
  use_metadata:
    required: false
    description: "Get tags from git"
    default: "true"
  login:
    description: 'Docker login'
    required: false
    default: ''
  password:
    description: 'Docker password'
    required: false
    default: ''
outputs:
  success:
    description: "Docker image promoted"
    value: "${{ steps.result.outputs.success }}"
runs:
  using: "composite"
  steps:
    - name: Docker meta
      id: meta
      if: ${{ inputs.use_metadata }}
      uses: docker/metadata-action@v4
      with:
        # list of Docker images to use as base name for tags
        images: |
          ${{ inputs.registry }}/${{ inputs.organization }}/${{ inputs.repository }}
        # generate Docker tags based on the following events/attributes
        tags: |
          type=sha
          type=schedule
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=semver,pattern={{major}}
          type=raw,value=latest,enable={{is_default_branch}}
          type=sha,format=long
          ${{ inputs.to }}
        flavor: |
          latest=auto
        labels: |
          org.opencontainers.image.source=https://github.com/${{ inputs.organization }}/${{ inputs.repository }}

    - name: Login
      uses: docker/login-action@v2
      if: ${{ contains(inputs.registry, '.amazonaws.com') || ( inputs.login != '' && inputs.password != '' ) }}
      with:
        registry: ${{ inputs.registry }}
        username: ${{ inputs.login }}
        password: ${{ inputs.password }}

    - uses: edwardgeorge/jq-action@main
      id: tag
      with:
        compact: true
        raw-output: true
        input: ${{ steps.meta.outputs.json }}
        script: '.tags | map(select(test("sha-\\w{8,}"))) | first | match("sha-\\w{8,}") | .string'

    - name: Get `from` baseed on metadata
      if: ${{ inputs.from == '' }}
      id: from
      uses: docker/metadata-action@v4
      with:
        images: ${{ inputs.registry }}/${{ inputs.organization }}/${{ inputs.repository }}
        tags: type=sha

    - id: info
      shell: bash
      run: |
        echo "from=${{ inputs.from}}${{ steps.from.outputs.version }}" >> $GITHUB_OUTPUT
        if [[ "${{ inputs.use_metadata }}" ]]; then
          echo "to=${{ steps.meta.outputs.tags }}" >> $GITHUB_OUTPUT
        else
          echo "to=${{ inputs.to }}" >> $GITHUB_OUTPUT
        fi;

    - name: Promote
      id: promote
      shell: bash
      run: |
        docker pull ${{ inputs.registry }}/${{ inputs.organization }}/${{ inputs.repository }}:${{ steps.info.outputs.from }}
        TAGS=(${{ steps.info.outputs.to }})
        for value in "${TAGS[@]}"
        do
          docker tag ${{ inputs.registry }}/${{ inputs.organization }}/${{ inputs.repository }}:${{ steps.info.outputs.from }} ${value}
          docker push ${value}
        done

    - id: result
      shell: bash
      run: |
        echo "success=true" >> $GITHUB_OUTPUT
